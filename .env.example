# Leave blank to keep local generated DESIGN asset URLs.
PUBLIC_DESIGN_ASSET_BASE_URL=

# Use "external" only after DESIGN assets move to remote storage.
DESIGN_ASSET_PROVIDER=local

# Optional manifest of `{entryId}/{assetPath}` keys for external DESIGN validation.
DESIGN_ASSET_MANIFEST=
