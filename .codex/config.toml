# Project-local Codex defaults for OL HOME.
# Loaded only when this project is trusted by Codex.

model_reasoning_effort = "high"
approval_policy = "on-request"
sandbox_mode = "workspace-write"
project_doc_max_bytes = 65536

[features]
hooks = true
multi_agent = true
