# Allow routine OL HOME verification commands outside the sandbox when Codex requests them.
prefix_rule(
    pattern = ["npm", "run", "build"],
    decision = "allow",
    justification = "OL HOME build verification is expected after site, content, or config changes.",
    match = [
        "npm run build",
    ],
)

prefix_rule(
    pattern = ["npm", "run", "dev"],
    decision = "prompt",
    justification = "Starting the dev server is useful but may leave a long-running process.",
    match = [
        "npm run dev",
    ],
)

prefix_rule(
    pattern = ["git", "push", "origin", "main"],
    decision = "allow",
    justification = "User-requested OL HOME uploads use origin main as the default publishing target.",
    match = [
        "git push origin main",
    ],
)

prefix_rule(
    pattern = ["git", "push"],
    decision = "prompt",
    justification = "Non-default pushes should be confirmed before updating a remote repository.",
    match = [
        "git push",
    ],
)

prefix_rule(
    pattern = ["rm", "-rf"],
    decision = "forbidden",
    justification = "Use explicit file deletion through a reviewed patch or safer command.",
    match = [
        "rm -rf dist",
    ],
)
